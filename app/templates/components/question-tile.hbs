<li>
  {{question.author}}'s <br> question: {{question.post}} <br> notes: <br> {{question.notes}}
  {{#if isImageShowing}}
  <p><img src={{question.image}} alt="loading..." {{action 'imageHide'}}></p>
  {{else}}
    <button {{action 'imageShow'}}>Show image</button>
  {{/if}}
  {{update-question question=question update="update"}}
  <button {{action 'delete' question}}>Delete Question</button>
</li>
